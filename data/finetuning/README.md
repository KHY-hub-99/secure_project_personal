# [R4] 파인튜닝 학습 데이터
이 폴더에 LoRA 학습용 JSONL 파일을 저장합니다.

## 파일 목록
- `red_train.jsonl` — [R1] LoRA-Red ~500건
- `judge_train.jsonl` — [R1] LoRA-Judge ~2,000건
- `blue_train.jsonl` — [R3] LoRA-Blue ~1,500건

세부기획서 섹션 2-3 참조.
